# DESIGN.md — offerAI 智能面试官 · UI 原型设计文档

> 本文档基于 [.cursor/PRD.md](.cursor/PRD.md) 编写，是 UI 原型（HTML 页面）落地前的**唯一设计依据**。
> 本文只描述「页面名称 / 布局 / 组件 / 交互逻辑」，**不含实现代码**。确认后再据此生成 HTML 原型。

---

## 0. 全局设计规范（Design System）

### 0.1 形态与适配

- **形态**：响应式 H5 Web App，**移动端优先**（设计以 375×812 iPhone 尺寸为基准），桌面端居中展示手机容器。
- **导航**：底部 TabBar 主导航，沉浸式页面（面试进行、引导）隐藏 TabBar。

### 0.2 色彩

| 用途 | 色值 | 说明 |
|------|------|------|
| 品牌主色 | `#4F46E5`（靛蓝） | 按钮、高亮、进度、图标 |
| 品牌辅色 | `#7C3AED`（紫） | 渐变、强调、会员 |
| 成功 | `#16A34A` | 评分高、通过、完成 |
| 警告 | `#F59E0B` | 评分中、提醒 |
| 危险 | `#EF4444` | 评分低、错误、退出 |
| 中性文字 | `#111827` / `#6B7280` / `#9CA3AF` | 主 / 次 / 弱 |
| 背景 | `#F9FAFB`（页）/ `#FFFFFF`（卡片） | 浅灰底 + 白卡片 |

### 0.3 字体与排版

- 字体：系统默认栈（`-apple-system, PingFang SC, Segoe UI, Roboto…`）。
- 字号：标题 `20/17/15`，正文 `14`，辅助 `12`；行高 1.5。
- 圆角：卡片 `12px`，按钮 `10px`，输入框 `10px`，标签 `999px`。

### 0.4 通用组件

| 组件 | 说明 |
|------|------|
| 顶部导航栏 NavBar | 返回 + 标题 + 右侧操作 |
| 底部标签栏 TabBar | 5 个主入口（见 0.5） |
| 主按钮 / 次按钮 | 通栏、渐变主按钮 |
| 卡片 Card | 白底圆角，含标题/内容/角标 |
| 表单 Form | 标签 + 输入框/选择器/单选/多选 |
| 选择器 Picker | 底部弹出选择 |
| 进度条 Progress | 面试进度、简历分析进度 |
| 评分星标 / 分数徽章 | 评分展示 |
| 空态 / 加载态 / 错误态 | 统一占位 |
| 标签 Tag | 技能、偏好、分类 |
| 底部操作条 BottomBar | 沉浸式页常驻操作 |

### 0.5 导航结构（信息架构）

```
TabBar（5 项）
├── 首页       Home
├── 面试       Interview
├── 简历       Resume
├── 指导       Guidance
└── 我的       Mine
```

沉浸式页面（无 TabBar）：登录、新用户引导、面试进行、简历分析、结果报告、自我介绍编辑。

---

## 1. 登录与注册模块

### 1.1 登录/注册页

| 项 | 内容 |
|----|------|
| 用途 | 登录、注册、快捷登录入口 |
| 布局 | 上：品牌 Logo + Slogan；中：手机号输入区或切换账号密码；下：登录按钮 + 第三方登录 + 协议 |

**布局示意**

```
┌─────────────────────┐
│  NavBar（返回/跳过）  │
│                     │
│      offerAI        │
│   AI 智能面试官      │
│   Slogan 文案        │
│                     │
│  ┌─────────────────┐ │
│  │ 手机号  [输入框]  │ │
│  │ 验证码  [输入][获取]│ │
│  └─────────────────┘ │
│  [ 登录 / 注册 ] 大按钮 │
│  ···· 密码登录 ····    │
│  微信登录    QQ登录     │
│  勾选 同意《用户协议》《隐私政策》│
└─────────────────────┘
```

**核心组件**：手机号输入框、验证码输入框 + 倒计时按钮、主登录按钮、微信/QQ 图标按钮、协议勾选、切换登录方式链接。

**交互逻辑**：
1. 默认「手机号 + 验证码」登录；未注册手机号自动进入注册流程。
2. 点击「获取验证码」→ 校验手机号格式 → 发送 → 按钮变 60s 倒计时。
3. 点击「密码登录」→ 切换为「账号 + 密码」表单。
4. 微信/QQ 登录 → 拉起第三方授权 → 绑定手机号（若首次）。
5. 未勾选协议 → 拦截并提示；登录成功 → 判断是否新用户。

### 1.2 新用户引导页（1 分钟）

| 项 | 内容 |
|----|------|
| 用途 | 新用户首次登录后功能引导 |
| 布局 | 全屏轮播（3–4 屏）+ 底部指示点 + 跳过/下一步 |

**核心组件**：轮播插画、步骤指示点、跳过按钮、下一步按钮。

**交互逻辑**：
1. 3–4 屏横向滑动介绍核心功能（智能面试 / 简历优化 / 就业指导）。
2. 最后一屏「立即开始」→ 跳转信息登记；任意屏「跳过」→ 直达首页。

---

## 2. 信息登记模块

> 分步表单，进度条贯穿；所有子页共用顶部进度条 + 保存按钮。

### 2.1 信息登记 · 主入口/步骤概览

| 项 | 内容 |
|----|------|
| 用途 | 展示登记步骤，引导逐项填写 |
| 布局 | 顶部进度条 + 步骤列表（卡片）+ 底部「开始填写」 |

**核心组件**：步骤进度条、步骤卡片（图标 + 标题 + 完成状态）、开始按钮。

**交互逻辑**：
1. 步骤：个人信息 → 教育经历 → 实习/工作 → 技能证书 → 求职偏好 → 补充信息 → 智能自我介绍。
2. 已完成步骤打勾，点击任意步骤可跳转编辑。
3. 支持部分选填（标「选填」），跳过不影响后续。

### 2.2 基础个人信息页

**布局**：单列表单，字段分「必填/选填」。

**核心组件**：文本输入、单选（性别/求职身份）、城市选择器、日期选择器、薪资范围选择器、到岗时间选择器。

**字段**：姓名、性别、年龄、联系电话、邮箱、意向岗位、期望城市、期望薪资、到岗时间、求职身份（应届/往届/转行/在职跳槽）。

**交互逻辑**：底部「保存并下一步」；必填校验通过才可继续。

### 2.3 教育经历页

**字段**：最高学历、毕业院校、专业、入学/毕业时间、主修课程、荣誉/奖学金（选填）、成绩（选填）、校园项目/竞赛/社团。

**核心组件**：输入、下拉选择、多行文本、可增删的动态条目（荣誉、项目）。

**交互逻辑**：「+ 添加」动态增加条目；每条可删除；支持多条教育经历。

### 2.4 实习/工作经历页

**字段**：公司、岗位、任职时长、工作内容、负责板块、实操技能、常用工具、工作成果/业绩/项目案例、离职原因（选填，供 AI 追问）。

**核心组件**：动态条目卡片、富文本/多行输入、标签选择（技能/工具）。

**交互逻辑**：「+ 添加经历」；离职原因提示「用于 AI 模拟追问，可不填」。

### 2.5 技能与证书页

**字段**：专业技能、办公技能、语言能力、职业资格证、英语等级、计算机证书。

**核心组件**：标签输入（TagInput，输入回车成标签）、多选、等级选择器。

**交互逻辑**：技能用 Tag 输入，回车生成标签，可删除。

### 2.6 求职偏好设置页

**字段**：意向行业、企业类型、是否接受加班/出差/异地、面试轮次偏好（HR 初面/技术面/主管终面）、面试风格（严肃正式/轻松日常）。

**核心组件**：多选、开关 Switch、单选组、图标化选择卡片。

**交互逻辑**：开关/单选即时生效，底部保存。

### 2.7 补充信息页

**字段**：个人优缺点自评、职业规划、兴趣爱好、特长。

**核心组件**：多行文本输入（字数统计）。

### 2.8 智能自我介绍页（核心）

| 项 | 内容 |
|----|------|
| 用途 | AI 一键生成 + 编辑 + 润色自我介绍 |
| 布局 | 上：时长/场景切换；中：文本预览（可编辑）；下：操作按钮 |

**核心组件**：分段选择器（时长：1 分钟/3 分钟）、场景选择（校招/社招/转行/英文）、可编辑文本框、润色按钮、重新生成按钮、复制/保存按钮。

**交互逻辑**：
1. 进入即基于已填信息「一键生成」→ 加载态 → 显示文本。
2. 切换时长/场景 → 重新生成对应版本。
3. 「AI 润色」→ 优化当前文本表达。
4. 手动编辑后未保存提示；「复制」一键复制。

---

## 3. 模拟面试模块

### 3.1 面试配置页

| 项 | 内容 |
|----|------|
| 用途 | 选择面试模式与参数 |
| 布局 | 模式选择卡片 + 参数列表 + 底部开始按钮 |

**核心组件**：模式卡片（1 对 1 / 1 对多群面）、岗位选择器、轮次选择（HR/技术/终面）、风格选择、开始面试按钮。

**交互逻辑**：
1. 顶部切换「1 对 1」「1 对多群面」。
2. 选择岗位/轮次/风格（默认读取求职档案预填）。
3. 「开始面试」→ 校验档案 → 进入面试进行页。

### 3.2 面试进行页（1 对 1，核心沉浸页）

| 项 | 内容 |
|----|------|
| 用途 | 实时问答、追问、语音转文字 |
| 布局 | 上：AI 面试官信息条 + 进度；中：对话流；下：输入/语音操作区 |

**布局示意**

```
┌─────────────────────┐
│ AI 面试官 · 技术面    │
│ ▓▓▓▓▓░░░░ 进度 4/10 │
├─────────────────────┤
│  [AI] 请做个自我介绍  │
│  [我] 我是…          │
│  [AI] 追问：具体项目… │
│  ...对话气泡流...     │
│   ⏺ 语音识别中…      │
├─────────────────────┤
│  [🎤语音] [输入框] [发送] │
│  [换种说法] [跳过本题] [结束]│
└─────────────────────┘
```

**核心组件**：面试官信息条、进度条、聊天气泡（左右分列）、语音波形/转文字动画、文本输入框、语音按钮、发送按钮、快捷操作按钮。

**交互逻辑**：
1. AI 发起开场提问；用户文字/语音回答（语音经 ASR 转文字后上屏）。
2. AI 基于回答**追问**或进入下一题；答题实时进度推进。
3. 「换种说法」重答当前题；「跳过本题」跳下一题；「结束」弹确认。
4. 网络中断：输入内容本地暂存，恢复后不丢失。

### 3.3 群面模拟页（1 对多）

**布局**：多面试官头像条 + 同一对话流；系统标注「面试官 A/B/C 提问」。

**核心组件**：多头像条、身份标签、对话流、作答倒计时。

**交互逻辑**：
1. 多位 AI 面试官轮流/穿插提问，气泡标注提问者。
2. 回答时展示「抢答倒计时」营造群面氛围（MVP 可简化）。

### 3.4 面试结果/评分页

| 项 | 内容 |
|----|------|
| 用途 | 面试结束后展示评分与改进建议 |
| 布局 | 上：总分 + 雷达/维度；中：逐题点评列表；下：继续/查看报告 |

**核心组件**：总分环、维度评分条（内容/逻辑/表达/专业）、逐题卡片（题目 + 我的回答摘要 + AI 点评 + 建议）、示范答案折叠、查看完整报告按钮。

**交互逻辑**：
1. 生成中动画 → 展示总分与各维度分。
2. 逐题卡片可展开看 AI 点评与改进建议。
3. 「查看完整报告」进入报告页；「再练一次」返回配置页。

### 3.5 面试报告页

**布局**：报告头部（岗位/时间/总分）→ 能力雷达图 → 逐题详情 → 整体评价 → 3 条核心建议 → 下次练习建议。

**核心组件**：雷达图、得分表格、建议列表、分享按钮。

**交互逻辑**：支持分享（P2）、导出。

---

## 4. 简历优化模块

### 4.1 简历上传页

**布局**：上传区（点击/拖拽）+ 已上传文件列表 + 底部「开始分析」。

**核心组件**：上传卡片（虚线框 + 图标 + 文案）、文件条目（图标 + 名称 + 大小 + 删除）、开始分析按钮。

**交互逻辑**：
1. 支持 PDF/Word 上传，限制大小/页数。
2. 上传中显示进度；失败可重试。
3. 点击「开始分析」→ 进入分析中状态。

### 4.2 简历分析中页

**核心组件**：分析进度动画 + 阶段提示（解析 → 排版 → 表达 → 亮点 → 匹配度）。

**交互逻辑**：分阶段进度，完成后自动跳转结果页。

### 4.3 简历优化结果页

**布局**：总评分 + 四维度（排版/表达/亮点/匹配度）+ 逐条建议列表 + 底部「生成优化版」。

**核心组件**：评分环、维度条、建议卡片（问题定位 + 优化建议）、一键生成按钮。

**交互逻辑**：
1. 展示总评与分维度得分。
2. 建议分「排版 / 表达 / 亮点 / 匹配度」四类，可筛选查看。
3. 「生成优化版简历」→ AI 输出可直接修改的优化版本。

### 4.4 优化版简历预览页

**核心组件**：简历预览（文档式）+ 编辑入口 + 复制/下载。

**交互逻辑**：展示优化版简历，支持复制文本或下载（P1）。

---

## 5. 就业指导模块

### 5.1 就业指导首页

**布局**：功能入口卡片（心理小课堂 / 情感陪伴 / 正能量推荐）+ 内容流。

**核心组件**：入口卡片、内容列表。

### 5.2 心理小课堂页

**核心组件**：文章/视频列表、分类标签。

### 5.3 AI 情感陪伴页

| 项 | 内容 |
|----|------|
| 用途 | 压力疏导、鼓励、陪伴对话 |
| 布局 | 顶部鼓励文案 + 对话流 + 输入区 |

**核心组件**：对话气泡（AI 陪伴风格）、快捷倾诉话题标签、输入区。

**交互逻辑**：与面试对话类似，但风格偏安抚、鼓励；提供「聊聊天」「舒缓压力」等快捷话题。

### 5.4 正能量内容推荐页

**核心组件**：励志音乐卡片、正能量视频卡片、心态调节文章卡片。

---

## 6. 我的（个人中心）

**布局**：头像 + 昵称 + 会员状态 → 数据概览（面试次数/简历优化数）→ 功能列表（我的报告、求职档案、会员、设置、隐私）。

**核心组件**：用户信息卡、数据统计卡、列表项、退出登录。

**交互逻辑**：
1. 数据概览点击跳对应记录。
2. 「求职档案」跳登记流程；「会员」跳购买；「设置」含通知、隐私、账号注销。

---

## 7. 后期规划模块（占位）

| 页面 | 说明 |
|------|------|
| 真人模拟面试 | 预约真人面试官，MVP 阶段为规划入口占位 |
| 真人问答墙 | 向行业前辈提问，规划占位 |
| 求职社区 / 面经 | 内容社区，规划占位 |

**交互逻辑**：当前版本以「敬请期待」占位卡呈现。

---

## 8. 全局交互与状态规范

| 场景 | 规范 |
|------|------|
| 加载态 | 按钮 loading / 骨架屏 / 进度动画 |
| 空态 | 插画 + 引导文案 + 行动按钮 |
| 错误态 | 文案 + 重试按钮 |
| 退出确认 | 沉浸式面试/编辑页离开时弹确认 |
| Toast | 操作反馈，2s 自动消失 |
| 返回 | 有未保存内容时二次确认 |

---

## 9. 页面清单总表

| 编号 | 页面 | 所属模块 | 有无 TabBar | 优先级 |
|------|------|----------|-------------|--------|
| 1 | 登录/注册页 | 登录 | 无 | P0 |
| 2 | 新用户引导页 | 登录 | 无 | P0 |
| 3 | 首页 | 首页 | 有 | P0 |
| 4 | 信息登记（步骤概览+6 子页） | 信息登记 | 有 | P0 |
| 5 | 智能自我介绍页 | 信息登记 | 无 | P0 |
| 6 | 面试配置页 | 模拟面试 | 有 | P0 |
| 7 | 面试进行页（1 对 1） | 模拟面试 | 无 | P0 |
| 8 | 群面模拟页 | 模拟面试 | 无 | P1 |
| 9 | 面试结果/评分页 | 模拟面试 | 无 | P0 |
| 10 | 面试报告页 | 模拟面试 | 无 | P0 |
| 11 | 简历上传页 | 简历优化 | 有 | P0 |
| 12 | 简历分析中页 | 简历优化 | 无 | P0 |
| 13 | 简历优化结果页 | 简历优化 | 无 | P0 |
| 14 | 优化版简历预览页 | 简历优化 | 无 | P1 |
| 15 | 就业指导首页 | 就业指导 | 有 | P1 |
| 16 | 心理小课堂 / 情感陪伴 / 正能量 | 就业指导 | 无 | P1 |
| 17 | 我的（个人中心） | 我的 | 有 | P0 |
| 18 | 后期规划占位页 | 规划 | 有 | P2 |

---

## 10. 待确认事项

1. **主色**：暂定靛蓝 `#4F46E5` + 紫 `#7C3AED` 渐变，是否契合品牌？
2. **导航**：底部 TabBar 5 项（首页/面试/简历/指导/我的），是否符合预期？
3. **面试进行页**：是否保留「换种说法 / 跳过本题 / 结束」三个快捷操作按钮？
4. **信息登记**：7 步分步表单，步数与顺序是否需要调整？

> 确认后，我将据此逐页生成 HTML 原型（静态高保真，不含后端逻辑）。
